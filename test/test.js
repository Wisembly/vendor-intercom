(function () {
	describe('Intercom test suite', function () {

		describe('General API', function () {
			it('should be defined', function () {
				expect(window.WisemblyIntercom).to.be.an('object');
			});
		});

	});
}());
